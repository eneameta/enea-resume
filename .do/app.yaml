name: enea-resume
   static_sites:
     - name: web
       github:
         repo: eneameta/enea-resume
         branch: main
       source_dir: /
       index_document: index.html
       output_dir: /
